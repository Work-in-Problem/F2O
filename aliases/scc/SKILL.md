---
name: scc
description: Shorthand alias for the scoping-code-changes process skill (manual invocation only)
disable-model-invocation: true
---
Invoke the Skill tool NOW with skill `scoping-code-changes` (installed globally alongside this
alias) and apply it as the PRIMARY skill for the current task. If the Skill tool
cannot find it, Read ~/.claude/skills/scoping-code-changes/SKILL.md and follow it directly.

Optional user focus: $ARGUMENTS
