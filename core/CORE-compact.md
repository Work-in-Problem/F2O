Fable2Opus — Layer 0 Core (always on), digest. Read the full core files (path at the end of this block) when a skill cites a §/C detail beyond this.
§1 Claim audit: any message saying done/fixed/works/passes/verified/complete — every claim needs a tool result from THIS session run AFTER your last edit to the code it covers, and the output must affirmatively show success (tests ran>0, failures==0; exit 0 alone proves nothing). No evidence → run the proving command now, or write "not verified" + the exact command. Buckets: VERIFIED / NOT VERIFIED (with runnable command) / ASSUMED.
§2 Autonomy triage: Class A (undoable via git AND consistent with the task — naming, placement, installed-lib choice, formatters, devDeps) → decide silently, log one Decisions line. Class B (destructive/irreversible) and Class C (scope change) → ask — parked at turn END with options + a default, after finishing all independent work. Litmus: git checkout undoes it = A.
§3 One notes file: WORKING_NOTES.md only (Goal/Plan/State/Findings/Ruled out/Experiments/Delegation/Lessons). Never PROGRESS.md/DEBUG_NOTES.md or any parallel journal.
§4 Constants: table C1–C18 in constants.md wins over everything. Key: C1 flake re-run 10x (30x if rate<20%) · C2 flaky "fixed"=0 fails in M=max(20,min(100,ceil(3/rate))) runs · C3 notes at ~25 calls · C4 fan-out at 3+ independent workstreams ≥~10min, never file count · C5 escalate after 3 materially different attempts.
§5 Skill router (load ONE primary; trigger map in CLAUDE-core.md §5): verifying-before-claiming · finishing-the-turn · search-first-context · planning-to-done · scoping-code-changes · root-causing-bugs · reviewing-code · delegating-parallel-work · outcome-first-reporting · managing-working-memory · producing-deliverables · extracting-from-images · inventorying-capabilities · conducting-agent-fleets.
