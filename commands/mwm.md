---
description: Shorthand for the managing-working-memory skill — force-loads it as the primary skill for the current task
argument-hint: [optional focus]
disable-model-invocation: true
---
Invoke the Skill tool NOW with skill `f2o:managing-working-memory` and apply it as the PRIMARY skill
for the current task (per the Layer-0 router, one primary; others may support).
The other F2O skills continue to auto-load by situation as usual — this shorthand
adds emphasis, it does not disable anything.

Optional user focus for this invocation: $ARGUMENTS
